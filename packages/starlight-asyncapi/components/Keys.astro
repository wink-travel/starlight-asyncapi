<div class="sl-asyncapi-keys">
  <slot />
</div>

<style>
  .sl-asyncapi-keys {
    column-gap: 1rem;
    display: grid;
    grid-template-columns: fit-content(30%) minmax(0, 1fr);
  }

  @media (min-width: 40rem) {
    .sl-asyncapi-keys {
      grid-template-columns: minmax(8rem, 30%) minmax(0, 1fr);
    }
  }

  .sl-asyncapi-keys > :global(:not(.sl-asyncapi-key)) {
    grid-column: 1 / -1;
  }
</style>
